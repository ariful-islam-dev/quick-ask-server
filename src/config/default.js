const config = {
    totalItems: 0,
    page: 1,
    limit: 10,
    sortType: "asc",
    sortBy: "updatedAt",
    search: ""
};

const defaultConfig = Object.freeze(config)
export default defaultConfig;